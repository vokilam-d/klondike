import { CommonRequestInterceptor } from './common-request.interceptor';

describe('CommonRequestInterceptor', () => {
  it('should be defined', () => {
    expect(new CommonRequestInterceptor()).toBeDefined();
  });
});
